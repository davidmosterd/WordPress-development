Using the scripts global
--------------------------

Add */Users/$USER/WordPress-development* to the PATH variable in *~/.bash_profile* to use the scripts global.

Example .bash_profile:

```Bash  
PATH=/Users/$USER/WordPress-development:$PATH
```
